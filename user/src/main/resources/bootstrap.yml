spring:
  application:
    name: user
  profiles:
    active: dev #设置SpringBoot生效配置文件
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: 127.0.0.1:8848
        file-extension: yaml
